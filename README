Hipster In Training is (or will eventually be) a collection of utilities that I find useful in converting my development box from Windows to Linux.  At the time of creation, it only includes a script to convert PuTTy settings from Windows to Linux, but I will add more utilities as I use them.